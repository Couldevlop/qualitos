package com.openlab.qualitos.quality.notifications.domain;

/** Sévérité/nature d'une notification in-app (pilote l'icône et la couleur côté UI). */
public enum NotificationType {
    INFO,
    SUCCESS,
    WARNING,
    ALERT
}
