<?xml version="1.0" encoding="UTF-8"?>
<!--
  Observabilité (CLAUDE.md §14, §22.9) — logging structuré.
  Console lisible par défaut (dev/test/CI), JSON une-ligne (LogstashEncoder) sous
  profils 'prod' ou 'json-logs'. MDC inclus (tenant_id, correlation_id).
  Jamais de PII en clair.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="prod | json-logs">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"service":"api-iot-hub"}</customFields>
                <timeZone>UTC</timeZone>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="!prod &amp; !json-logs">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
