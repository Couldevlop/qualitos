# Audit « stub vs réel » — capacités IA / IoT / Blockchain-Crypto

> Date : 2026-05-29. Méthode : lecture du code décisif (logique effective vs simulée), pas de la doc.
> Classement : **RÉEL** (logique effective) · **STUB** (structure, sortie factice/déterministe) ·
> **SCAFFOLD** (squelette opt-in / non branché) · **DOCUMENTAIRE** (config/suite définie, non activée) ·
> **ABSENT** (aucun code).

## Tableau consolidé

| Capacité | Verdict | Preuve (code décisif) |
|---|---|---|
| **Crypto** ML-DSA-65 (signature) | RÉEL | `BouncyCastleSignatureProvider.java:119` `Signature.getInstance("ML-DSA", BC)` ; test « signature > 2000 octets » |
| **Crypto** ML-KEM-768 (KEM) | RÉEL | `BouncyCastleKemProvider.java:46` `MLKEMParameters.ml_kem_768` ; round-trip testé |
| **HybridSignatureService** (Ed25519+ML-DSA) | RÉEL | `HybridSignatureService.java:50-65` signe 2× réellement, vérifie tout |
| **Ancrage Phase A** (Merkle + reçus chaînés) | RÉEL | `SignedAnchorAdapter.java:50-85` + `MerkleTree` + `AnchorVerificationService` (7 tests) |
| **LLM providers** Ollama/Anthropic/Mistral | RÉEL | appels HTTP réels (`ollama_provider.py:40`, `anthropic_provider.py:47`, `mistral_provider.py:44`) |
| **RAG** Qdrant + BGE-M3 | RÉEL (fallback dev) | `rag_query.py:65-117` ; fallback in-memory + embedder déterministe si pas de GPU |
| **NLQ** text-to-SQL | RÉEL | `nlq_ask.py:102-151` + `sqlglot_validator.py` (SELECT-only, allow-list, tenant) + executor read-only |
| **AiGatewayClient** (relais engine→ai-service) | RÉEL | `AiGatewayClient.java:62-76` POST `/v1/ai/complete` |
| **IoT Device Registry** | RÉEL | JPA transactionnel, ISA-95, X.509 fingerprint, RLS tenant (`DeviceController`, `V1__iot_schema.sql`) |
| **IoT Ingestion télémétrie** | RÉEL | `IngestTelemetryUseCase.java:40-56` save JPA + batch ; rollups SQL standard (`/devices/{id}/telemetry/rollup`, `JpaTelemetryRepository.rollupByDevice` date_trunc portable PG/H2) ; **TimescaleDB désormais activé** (continuous aggregate `iot_telemetry_hourly` + retention 90j + compression 7j, migration **V2** gardée `IF EXISTS(timescaledb)` → inerte sur PG simple / H2) |
| **Chaincode Go** AnchorAudit/VerifyEvidence | RÉEL | `qualitos_anchor.go:37-80` GetState/PutState réels, clé composite tenant |
| **Modules AI Act** aiconformity/aiincidents/aipmm | RÉEL (CRUD réglementaire) | workflows d'état ; **ce sont des modules de _gouvernance_ de l'IA (AI Act), pas des features prédictives** — pas d'appel LLM, et c'est normal |
| **Vision 5S (YOLOv8)** | ~~STUB~~ → **RÉEL (ONNX + fallback), chemin ONNX exercé en test** | ✅ `OnnxInferenceBackend` (vraie inférence si modèle fourni via `VISION5S_ONNX_MODEL_PATH`, sinon fallback stub). **Modèle jouet ONNX committé** (`models/vision5s-toy.onnx`, 368 o, généré par `scripts/export_5s_model.py`) → le **vrai chemin d'inférence ONNX** (session `onnxruntime` réelle) est désormais **chargé et exécuté en test** (`test_onnx_real_model.py`, conditionnel `importorskip`), plus le mock historique. **Modèle de prod = à fournir** : YOLOv8 fine-tuné 5S (pipeline d'entraînement décrit dans le runbook §2.3, dataset non fourni). |
| **IoT protocoles terrain** (OPC-UA/MQTT/HL7/LoRaWAN/Sparkplug/Modbus…) | ~~STUB~~ → **MQTT + OPC-UA + HL7 FHIR + LoRaWAN + Sparkplug B + Modbus RÉELS** | ✅ Connecteurs MQTT (Paho v5), OPC-UA (Milo 0.6.16), HL7 FHIR R5 (REST entrant), **LoRaWAN** (ADR 0026, `POST /api/v1/iot/lorawan/uplink`), **Sparkplug B** (ADR 0027, JSON NDATA/DDATA, `POST /api/v1/iot/sparkplug`) et **Modbus TCP/RTU** (ADR 0028 : lecture structurée `{deviceCode, readings[{register, metric, value, unit}], time}` décodée par la passerelle Edge, `POST /api/v1/iot/modbus`). Tous : décodeur pur robuste (entrée pourrie ignorée+comptée), handler tenant-agnostique via `findUniqueByCode` **fail-closed** cross-tenant (chemin MQTT, §18.2 #2), gating off par défaut. Décodage protobuf/fil brut (Eclipse Tahu / lib Modbus) délégué. Reste STUB : DICOM. |
| **IoT Digital Twin / Shadow** | ~~STUB~~ → **RÉEL** | ✅ Round-trip JSON réel (Jackson) dans `JpaDeviceRepository` + `DeviceShadow` (domaine pur, faces reported/desired type Ditto) ; l'ingestion alimente la face `reported` (dernière valeur/métrique), endpoints `GET /devices/{id}/shadow` + `PATCH /devices/{id}/shadow/desired`. Round-trip + état rapporté prouvés en intégration H2. |
| **Federated learning** (Flower) | SCAFFOLD | `opt_in_federated_client.py:27` retourne `samples_used=0` synthétique |
| **IoT Stream rule engine → NC** | ~~SCAFFOLD~~ → **RÉEL (in-engine)** | ✅ Résolu (ADR 0016, V65) : détection dans `TelemetryIngestionService` → CAPA `IOT_ALERT` (seuils configurables `/api/v1/iot/thresholds`, anti-spam, tenant via JWT). Le chemin `api-iot-hub→/nc/from-iot` reste hors périmètre. |
| **Ancrage Phase B (Fabric)** | SCAFFOLD | `FabricAnchorService` propose→endorse→submit réel **mais** `@Profile("fabric")` désactivé, **0 test**, **réseau Fabric absent** (réutilise fabric-samples externe), fallback auto → Phase A |
| **TLS hybride X25519+ML-KEM-768 (WS5)** | ~~DOCUMENTAIRE~~ → **RÉEL (profil optionnel)** | ✅ Résolu : BC 1.81 + `bctls`, `HybridTlsConfig @Profile("tls")` (BCJSSE + `jdk.tls.namedGroups=X25519MLKEM768`), `application-tls.yml`. **Preuve** : `HybridTlsHandshakeTest` (handshake TLS 1.3 hybride en loopback). OFF par défaut. |
| **Edge Gateway K3s (§9.5)** | ~~ABSENT~~ → **RÉEL (composant d'inférence)** | ✅ ADR 0030 : composant Python `infra/edge/inference/` (hexagonal) — buffer **store-and-forward** (FIFO, borné, rejeu ordonné, **mode déconnecté**, persistance JSONL) + orchestrateur **ONNX-ou-repli déterministe** (import paresseux d'onnxruntime, dégradation gracieuse, calqué Vision 5S). **Preuve** : 37 tests pytest (96 % couv., domaine ~100 %) — logique store-and-forward + repli testée 100 % sans dépendance lourde, chemin ONNX par mock. Conteneur distroless multi-arch + manifeste K3s `edge-inference.yaml` (durci, aucun entrant). Le **modèle ONNX réel** et le **runtime long-running** (souscription MQTT → orchestrateur) restent à fournir. Le broker mosquitto local (bridge mTLS sortant) préexistait. |
| **Prédiction KPI (§6.5, §12)** | ~~ABSENT~~ → **RÉEL (Holt-Winters) ; LSTM/Prophet OPT-IN câblés** | ✅ ADR 0023 : lissage exponentiel Holt-Winters (niveau+tendance+saisonnalité additive, NumPy pur, params par grid-search) dans `forecasting.py`, `POST /v1/ai/predict/kpi` + relais engine `forecast/ForecastController` (`POST /api/v1/ai/forecast/kpi`) + UI `/forecast`. **ADR 0031** : `model=holt_winters` (défaut réel) \| `prophet` \| `lstm` — backends lourds **opt-in, import paresseux** (`domain/service/ml_backends/forecast_prophet.py`, `forecast_lstm.py`, extra `ml`), même contrat `KpiForecast` ; indisponible → **422** « extra ml » (jamais de faux résultat). TFT reste à brancher. |
| **Anomalies SPC / Nelson rules (§3.4, §12.1)** | ~~ABSENT~~ → **RÉEL (branché engine)** | ✅ ADR 0018 : `domain/service/spc_rules.py` (limites I-chart MR̄/d2 + 8 règles Nelson, NumPy), use case `SpcDetectUseCase`, endpoint `POST /v1/ai/spc/analyze` ; 19 tests. **Branché côté engine (2026-06-01)** : `AiGatewayClient.detectSpc` (garde-fou LLM04 réutilisé) + `spc/SpcController`+`SpcService`+`SpcDto` (`POST /api/v1/ai/spc/analyze`), 6 tests ; E2E vérifié (NELSON_1/2 détectées). |
| **Anomalies ML (Isolation Forest/Autoencoder)** | ~~ABSENT~~ → **RÉEL (branché engine)** | ✅ ADR 0022 : détection non-supervisée **multivariée** (matrice échantillons × features), NumPy pur. `domain/service/isolation_forest.py` (Isolation Forest fidèle à Liu-Ting-Zhou 2008 : iTrees, `E[h(x)]`, score `2^(-E[h(x)]/c(n))`, déterministe seedé) + `domain/service/reconstruction.py` (auto-encodeur **linéaire** par ACP/SVD, erreur de reconstruction L2 + feature dominante). Use case `AnomalyDetectUseCase`, endpoint `POST /v1/ai/anomaly/detect` ; couverture nouveaux modules ≈ 98 %. **Branché côté engine** : `AiGatewayClient.detectAnomaly` (garde-fou LLM04, op « anomaly ») + `anomaly/AnomalyController`+`AnomalyService`+`AnomalyDto` (`POST /api/v1/ai/anomaly/detect`), tests verts (`verify` JaCoCo OK). Reste : UI, dérivation auto IoT/`kpi_measurements`, ouverture CAPA sur anomalie. |
| **Clustering NC (DBSCAN défaut ; HDBSCAN)** | ~~ABSENT~~ → **RÉEL (DBSCAN) ; HDBSCAN OPT-IN câblé** | ✅ DBSCAN TF-IDF NumPy pur (défaut réel, `nc_clustering.py`, `POST /v1/ai/predict/nc-clusters`). **ADR 0031** : `method=dbscan` (défaut) \| `hdbscan` — backend lourd **opt-in, import paresseux** (`domain/service/ml_backends/cluster_hdbscan.py`, réutilise la même vectorisation, extra `ml`), même contrat `NcClusteringResult` ; indisponible → **422** « extra ml ». |
| **Explicabilité SHAP** | ~~ABSENT~~ → **RÉEL (Kernel SHAP)** | ✅ ADR 0024 : `domain/service/shap_kernel.py` (Kernel SHAP model-agnostic, régression pondérée sur coalitions, espérance d'imputation exacte si abordable, NumPy pur). Branché sur l'Isolation Forest (`AnomalyExplainUseCase`, `POST /v1/ai/anomaly/explain`) + relais engine + UI « Pourquoi ? » (graphe des contributions). Propriété d'efficacité Σφ = score − base testée. |
| **NLP réclamations (sentiment + classification)** | ~~ABSENT~~ → **RÉEL (lexical) ; BERT OPT-IN câblé** | ✅ `domain/service/complaint_nlp.py` : sentiment lexical pondéré (négation, intensifieurs) + classification par termes-graines + détection critique, pur FR/EN. `POST /v1/ai/complaints/analyze` + relais engine `complaintnlp/` + UI `/complaints-nlp`. **ADR 0031** : `backend=lexical` (défaut) \| `bert` — Transformer HF **opt-in, import paresseux** (`domain/service/ml_backends/sentiment_bert.py`, extra `ml`), même contrat ; indisponible → **422** « extra ml ». |
| **Transcription audio (Whisper, §3.3/§15.3)** | ~~ABSENT~~ → **OPT-IN câblé (501 si extra ml absent)** | ✅ ADR 0031 : `POST /v1/ai/transcribe` (`UploadFile`, garde-fou taille 25 Mo, tenant via JWT) → `domain/service/ml_backends/transcribe_whisper.py` (vrai Whisper, import paresseux, extra `ml`). **Pas de chemin léger** : indisponible → **501** « transcription non disponible (extra ml) », jamais de faux texte. |
| **Standards Hub — génération doc normatif IA (§8.8)** | ~~STUB (1 paragraphe)~~ → **RÉEL (doc complet + validation humaine)** | ✅ ADR 0032 : `GenerateNormDocUseCase` (ai-service, génération **multi-sections** section-par-section via `AIProvider`, garde-fous PII/injection) `POST /v1/ai/standards/generate-document` ; côté engine module hexagonal `standards.normdoc` (agrégat `NormativeDocument` `BROUILLON_IA→EN_VALIDATION→APPROUVE`, **signature humaine obligatoire** + approbateur≠soumetteur, generator branché sur `AiGatewayClient` — **aucun doc en dur**), `POST /api/v1/standards/norm-documents/generate` + workflow submit/approve/reject, V88, golden-master. Tenant+acteur via JWT (§18.2 #2/#5). |

## Lecture d'ensemble

- **Socle confiance (crypto PQ + notarisation signée Phase A)** : réellement production-ready et testé. C'est le point le plus solide.
- **Pipeline IA = LLM générique** (providers + RAG + NLQ) réel ; **toute l'IA « ML classique/prédictive » annoncée au §12 est absente** (0 LSTM, 0 anomalie, 0 clustering, 0 SHAP).
- **IoT** : registre + ingestion réels ; **universalité protocoles, edge, twin, et chaîne capteur→NC = stub/scaffold/absent**.
- **Fabric (Phase B)** : code livré et compilable, mais **non opérationnel** (profil off, pas de réseau, pas de tests).

## Dette technique priorisée

**P0 — écarts « annoncé/livré » vs réel (crédibilité & sécurité)**
1. ~~**TLS hybride**~~ → ✅ **Résolu** (2026-05-29) : profil `tls` optionnel (BC 1.81 + BCJSSE + `X25519MLKEM768`), handshake hybride prouvé par test. OFF par défaut.
2. **blockchain-service** : ✅ tests unitaires ajoutés (`FabricAnchorServiceTest`, 7 cas). **Reste** : réseau Fabric réel en CI (Testcontainers) — Phase B toujours non opérationnelle (profil `fabric` off), Phase A reste la cible prod.

**P1 — profondeur des features phares**
3. ~~**Chaîne capteur→NC cassée**~~ → ✅ **Résolu** (2026-05-29, ADR 0016) : détection de seuil in-engine ouvrant une CAPA `IOT_ALERT`, seuils configurables, anti-spam. Reste : enrichir §9.9 (lien FMEA, cycle PDCA auto).
4. ~~**Vision 5S**~~ → ✅ **Résolu + chemin réel testé** : backend ONNX réel + fallback ; **modèle jouet ONNX committé** + script d'export reproductible (`scripts/export_5s_model.py`) → le chemin ONNX réel est **chargé/exécuté en test** (`test_onnx_real_model.py`, ADR 0029). Reste à fournir/entraîner un `.onnx` 5S **de production** (YOLOv8, pipeline décrit dans le runbook §2.3, dataset non fourni).
5. ~~**Connecteurs protocoles IoT**~~ → ✅ **MQTT + OPC-UA + HL7 FHIR + LoRaWAN + Modbus livrés** (Paho v5, Milo 0.6.16, FHIR R5 REST, LoRaWAN TTN/ChirpStack webhook, Modbus TCP/RTU lecture structurée — ADR 0028 ; tenant depuis le registre device, fail-closed). Reste : Sparkplug B / DICOM.

**P2 — capacités ML annoncées absentes**
6. **IA prédictive** : ⏳ **Entamé** (2026-05-31, ADR 0018) — détection d'anomalies **SPC réelle** (8 règles Nelson + limites de contrôle, `POST /v1/ai/spc/analyze`, 19 tests). ✅ **SPC branché côté engine** (2026-06-01) : `AiGatewayClient.detectSpc` + `spc/SpcController` (`POST /api/v1/ai/spc/analyze`, 6 tests, E2E OK). ✅ **Anomalies ML non-supervisées multivariées branchées** (2026-06-13, ADR 0022) : Isolation Forest + reconstruction ACP (NumPy pur, déterministe), `POST /v1/ai/anomaly/detect` + `anomaly/AnomalyController` (`POST /api/v1/ai/anomaly/detect`), `AiGatewayClient.detectAnomaly`. Reste : prédiction KPI (LSTM/Prophet) ; UI SPC + anomalies ; dérivation auto depuis `kpi_measurements`/IoT.
7. **Federated learning** : laisser en opt-in mais ne pas survendre.

**P3 — hygiène**
8. Digital Twin réhydraté vide (TODO P4).
9. ~~Garde-fous IA (rate-limit / circuit breaker / quotas budgétaires) non branchés sur le chemin LLM.~~ → ✅ **Résolu** (2026-05-31, ADR 0017) : `AiGuard`/`TokenBucketAiGuard` cloisonné par tenant (token bucket débit/min + quota journalier + disjoncteur + borne de taille de prompt) branché dans `AiGatewayClient.complete`/`askNlq` avant tout départ réseau ; rejets mappés 429/413/503 (`GlobalExceptionHandler`) ; paramétrable `qualitos.ai.guard.*`. État en mémoire/nœud — port `AiGuard` swappable Redis pour le cluster.
10. Couverture front : ✅ **fortement améliorée** — +35 specs services (67 → **271 tests**, tous les services métier couverts). Reste : couches composant (`*-list`/`*-view`) + `core/theme`.
